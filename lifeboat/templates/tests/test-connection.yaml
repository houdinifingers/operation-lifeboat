apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lifeboat.fullname" . }}-test-connection"
  labels:
    {{- include "lifeboat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lifeboat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
