apiVersion: v1
kind: Service
metadata:
  name: {{ include "lifeboat.fullname" . }}
  labels:
    {{- include "lifeboat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lifeboat.selectorLabels" . | nindent 4 }}
